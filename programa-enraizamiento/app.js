function renderProgramas(){selects();let ks=keys(),e=$('listaProgramas');if(!ks.length){e.innerHTML='<div class="vacio">Todavía no hay programas cargados.</div>';return}e.innerHTML=ks.map(k=>{let p=db.programas[k],t=totals(p),ac=String(db.activo)===k;return `<div class="programa-item"><div class="programa-top"><div><div class="programa-titulo">Programa Semana ${k} ${ac?'<span class="badge ok">Activo</span>':''}</div><div class="programa-meta">${p.items.length} líneas · ${fc(t.plan)} camas programadas</div><div class="programa-meta">Avance: ${fc(t.ap)} / ${fc(t.plan)} · ${t.ok}/${t.lineas} completas</div></div><div class="programa-acciones"><button class="btn-s btn-chico" onclick="activarSemana('${k}')">Usar</button><button class="btn-d btn-chico" onclick="eliminarSemana('${k}')">✕</button></div></div></div>`}).join('')}function kpis(t){return `<div class="kpi"><div class="v">${fc(t.plan)}</div><div class="t">Camas programadas</div></div><div class="kpi"><div class="v">${fc(t.ap)}</div><div class="t">Contabilizadas</div></div><div class="kpi"><div class="v">${fc(t.pend)}</div><div class="t">Pendientes</div></div><div class="kpi"><div class="v">${t.ok}/${t.lineas}</div><div class="t">Líneas completas</div></div>`}function renderRegistro(){selects();let p=activo(),e=$('listaItems');if(!p){$('kpisRegistro').innerHTML='';e.innerHTML='<div class="card vacio">Carga primero un programa.</div>';return}$('selSemana').value=String(db.activo);$('kpisRegistro').innerHTML=kpis(totals(p));let q=norm($('buscar').value),items=p.items.filter(i=>!q||norm(i.variedad).includes(q)||norm(i.nro).includes(q));e.innerHTML=items.map(i=>itemHTML(i)).join('')||'<div class="card vacio">Sin coincidencias.</div>'}function itemHTML(i){let r=restante(i),ok=r<=.0005,pct=n(i.camasProg)?Math.min(100,aplicado(i)/n(i.camasProg)*100):100,m=(i.movimientos||[]).map(x=>movHTML(i,x)).join('');return `<div class="item ${ok?'completo':''}"><div class="item-top"><div><div class="item-var">${esc(i.variedad)}</div><div class="programa-meta">Nro. ${esc(i.nro||'—')} · ${esc(i.prod||'')}</div><div class="item-plan">Programado: <b>${fc(i.camasProg)} camas</b> · ${fmt(i.esquejesProg)} esquejes</div></div><div class="item-status"><span class="badge ${ok?'ok':'warn'}">${ok?'Completo':'Pendiente'}</span><div class="restante">${ok?'0':fc(r)} camas</div></div></div><div class="progress"><div style="width:${pct}%"></div></div><button class="${ok?'btn-n':'btn-p'} btn-full" ${ok?'disabled':''} onclick="abrirModal('${i.id}')">${ok?'✓ Total programado cumplido':'＋ Agregar movimiento'}</button>${m?'<div class="mov-lista">'+m+'</div>':''}</div>`}function tipoNombre(t){return{siembra:'Siembra normal',generacion:'Generación diferente',sustitucion:'Sustitución',cancelacion:'Cancelación'}[t]||t}function movHTML(i,m){let d=`${fc(m.camas)} camas`;if(m.fecha)d+=` · ${m.fecha}`;if(m.clon)d+=` · ${esc(m.clon)}`;if(m.esquejes)d+=` · ${fmt(m.esquejes)} esq.`;if(m.faltantes)d+=` · ${fmt(m.faltantes)} falt.`;if(m.bloque||m.cama)d+=` · B${esc(m.bloque||'—')} / C${esc(m.cama||'—')}`;if(m.sustituto)d+=` · Sust. ${esc(m.sustituto)}`;if(m.obs)d+=` · ${esc(m.obs)}`;return `<div class="mov"><div class="mov-top"><div><div class="mov-tipo">${tipoNombre(m.tipo)}</div><div class="mov-det">${d}</div></div><div class="mov-acciones"><button class="btn-s btn-chico" onclick="abrirModal('${i.id}','${m.id}')">✎</button><button class="btn-d btn-chico" onclick="borrarMovimiento('${i.id}','${m.id}')">🗑</button></div></div></div>`}function buscarItem(id){return activo()?.items.find(i=>i.id===id)}function abrirModal(id,mid=null){let i=buscarItem(id),m=mid?(i.movimientos||[]).find(x=>x.id===mid):null;if(!i)return;itemActual=id;movEditando=mid;let r=restante(i,mid);$('modalTitulo').textContent=m?'Editar movimiento':'Registrar movimiento';$('modalSub').textContent=i.variedad;$('modalPlan').innerHTML=`Programado: <b>${fc(i.camasProg)} camas</b> / ${fmt(i.esquejesProg)} esquejes<br>Disponible: <b>${fc(r)} camas</b>`;$('movTipo').value=m?.tipo||'siembra';$('movClon').value=m?.clon||'';$('movSust').value=m?.sustituto||'';$('movFecha').value=m?.fecha||new Date().toISOString().slice(0,10);$('movCamas').value=m?.camas??'';$('movEsquejes').value=m?.esquejes??'';$('movFaltantes').value=m?.faltantes??'';$('movBloque').value=m?.bloque||'';$('movCama').value=m?.cama||'';$('movObs').value=m?.obs||'';$('movError').classList.remove('ver');tipoCambio();$('modalMov').classList.add('ver')}function cerrarModal(){$('modalMov').classList.remove('ver');itemActual=movEditando=null}function cerrarSiFondo(e){if(e.target===$('modalMov'))cerrarModal()}function tipoCambio(){let t=$('movTipo').value;$('grupoSust').style.display=t==='sustitucion'?'block':'none';let e=$('movInfo');if(t==='cancelacion'){e.textContent='La cancelación también consume camas pendientes del programa.';e.classList.add('ver')}else e.classList.remove('ver')}function validarCamasLive(){let i=buscarItem(itemActual);if(!i)return;let max=restante(i,movEditando),v=n($('movCamas').value),e=$('movError');if(v>max+.0005){e.textContent=`Solo quedan ${fc(max)} camas disponibles.`;e.classList.add('ver')}else e.classList.remove('ver')}function guardarMovimiento(){let i=buscarItem(itemActual),max=restante(i,movEditando),c=n($('movCamas').value),t=$('movTipo').value,e=$('movError');if(c<=0||c>max+.0005){e.textContent=c<=0?'Ingresa una cantidad de camas mayor que 0.':`Supera el total programado. Máximo: ${fc(max)} camas.`;e.classList.add('ver');return}let m={id:movEditando||uid(),tipo:t,clon:$('movClon').value.trim(),sustituto:$('movSust').value.trim(),fecha:$('movFecha').value,camas:c,esquejes:n($('movEsquejes').value),faltantes:n($('movFaltantes').value),bloque:$('movBloque').value.trim(),cama:$('movCama').value.trim(),obs:$('movObs').value.trim()};i.movimientos=i.movimientos||[];if(movEditando){let x=i.movimientos.findIndex(z=>z.id===movEditando);i.movimientos[x]=m}else i.movimientos.push(m);save();cerrarModal();renderRegistro();toast(restante(i)<=.0005?'✓ Total programado cumplido':'Movimiento guardado')}function borrarMovimiento(iid,mid){let i=buscarItem(iid);if(!i||!confirm('¿Eliminar este movimiento?'))return;i.movimientos=i.movimientos.filter(m=>m.id!==mid);save();renderRegistro()}function renderResumen(){selects();let k=$('selSemanaResumen').value||String(db.activo||''),p=db.programas[k];if(!p){$('kpisResumen').innerHTML='';$('tablaResumen').innerHTML='<div class="vacio">No hay programa.</div>';return}let t=totals(p);$('kpisResumen').innerHTML=`<div class="kpi"><div class="v">${fc(t.ap)}</div><div class="t">Camas contabilizadas</div></div><div class="kpi"><div class="v">${fmt(t.er)}</div><div class="t">Esquejes reales</div></div><div class="kpi"><div class="v">${fmt(t.fa)}</div><div class="t">Faltantes</div></div><div class="kpi"><div class="v">${t.ok}/${t.lineas}</div><div class="t">Líneas completas</div></div>`;$('tablaResumen').innerHTML=`<table><thead><tr><th>Variedad</th><th>Prog.</th><th>Contab.</th><th>Pend.</th><th>Esq. real</th><th>Falt.</th></tr></thead><tbody>${p.items.map(i=>{let er=0,fa=0;(i.movimientos||[]).forEach(m=>{er+=n(m.esquejes);fa+=n(m.faltantes)});return `<tr><td><b>${esc(i.variedad)}</b></td><td>${fc(i.camasProg)}</td><td>${fc(aplicado(i))}</td><td>${fc(restante(i))}</td><td>${fmt(er)}</td><td>${fmt(fa)}</td></tr>`}).join('')}</tbody></table>`}function exportarExcel(){let p=db.programas[$('selSemanaResumen').value];if(!p)return;let a=p.items.map(i=>({Semana:p.week,Nro:i.nro,Variedad:i.variedad,'Camas Prog.':i.camasProg,'Esquejes Prog.':i.esquejesProg,'Camas Contabilizadas':aplicado(i),'Camas Pendientes':restante(i)})),b=[];p.items.forEach(i=>(i.movimientos||[]).forEach(m=>b.push({Semana:p.week,Nro:i.nro,Variedad:i.variedad,Tipo:tipoNombre(m.tipo),'Clon / Generación':m.clon,'Sustituye por':m.sustituto,'Fecha real siembra':m.fecha,Camas:m.camas,'Esquejes reales':m.esquejes,Faltantes:m.faltantes,Bloque:m.bloque,Cama:m.cama,Observación:m.obs})));let w=XLSX.utils.book_new();XLSX.utils.book_append_sheet(w,XLSX.utils.json_to_sheet(a),'Programado');XLSX.utils.book_append_sheet(w,XLSX.utils.json_to_sheet(b),'Movimientos');XLSX.writeFile(w,`Programa_Enraizamiento_Sem_${p.week}.xlsx`)}selects();renderProgramas();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});